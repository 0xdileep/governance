# Ultra-minimal requirements for Lambda deployment
# Uses Pydantic v1 (pure Python, no Rust dependencies)

fastapi==0.68.0      # Last version compatible with Pydantic v1
pydantic==1.10.12    # Pure Python, no compiled extensions
uvicorn==0.15.0      # ASGI server (was missing - caused import error)
boto3==1.34.0        # AWS SDK
requests==2.31.0     # HTTP client
mangum==0.17.0       # ASGI adapter for Lambda
python-dotenv==1.0.0 # Environment variables
